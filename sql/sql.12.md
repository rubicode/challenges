# SQL Database Query

## challenge #12
Buatlah query SQL untuk menampilkan department yang memiliki lebih dari 1 karyawan dan total gaji department tersebut melebihi 100,000. Tampilkan `department_name`, `employee_count`, `total_salary`, dan `avg_salary`. Gunakan HAVING clause.

## database schema
```sql
-- Menggunakan tabel departments dan employees yang sama seperti soal sebelumnya
```

## expected output
```
department_name | employee_count | total_salary | avg_salary
----------------|----------------|--------------|------------
Engineering     | 3              | 175000.00    | 58333.33
```

## keyword
SELECT, JOIN, GROUP BY, HAVING, COUNT, SUM, AVG

## difficulty
Hard
