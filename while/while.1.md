# While Loop

## challenge #1
Buatlah perulangan while yang terus meminta input angka dari user sampai user memasukkan angka yang valid (antara 1-10). Gunakan prompt() untuk input dan tampilkan pesan error jika input tidak valid.

## driver code
```javascript
let angka;
// Tulis kode while loop di sini untuk validasi input
console.log("Angka valid yang dimasukkan: " + angka);
```

## output
```javascript
// Jika user input: abc, 15, 0, 5
// Maka output:
Input tidak valid, masukkan angka 1-10
Input tidak valid, masukkan angka 1-10  
Input tidak valid, masukkan angka 1-10
Angka valid yang dimasukkan: 5
```

## keyword
While, input validation, prompt, condition, user interaction
